const UserName = "ABC";

const user = {
    age:22
}

const COMMON = {
    cancel:"cancel",
    close:"close"
}

const ABOUTUS = {
    title:"title1"
}

const CONTACTUS = {
    title:"title2"
}

//export {user, UserName};
export default Object.seal(COMMON);